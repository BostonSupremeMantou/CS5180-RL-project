# weights/non_learning_agents

I reserved this spot in case I ever ship **learned or tuned artifacts** for hand-written policies. Right now my `flow_only` / `periodic` code doesn’t need saved weights, so it’s often empty.

## If I use it later

I’d probably drop something like a calibration file or ONNX export here and teach `src.evaluate` how to load it—but that’s hypothetical today.
