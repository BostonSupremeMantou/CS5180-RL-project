# outputs/smoke_tests/RL_agents

When I run a **tiny training smoke** per RL group, the short `train_metrics.csv` files land in subfolders mirroring the real `outputs/RL_agents/<group>/` layout.

## Why bother

If `g2` trains for 200 steps here without crashing, I’m pretty sure imports and shapes are sane before I kick off a 200k-step job.
