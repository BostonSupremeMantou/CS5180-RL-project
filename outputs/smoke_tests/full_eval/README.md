# outputs/smoke_tests/full_eval

Sometimes I run a **mini eval sweep** (few episodes, subset of policies) and stash the CSV here so I can diff against a full `eval_raw.csv` later.

## What I expect

- A shrunken `eval_raw.csv` and maybe a plot or two if I generated them in the same dry run.
