# outputs/non_learning_agents

When I evaluate **hand-written policies** (`flow_only`, `periodic`, …), I drop the CSVs here so I can diff them against RL later.

## What I expect

- One or more `eval_*.csv` files with per-episode metrics.
- Maybe nothing at all if I haven’t run those baselines on this machine yet—that’s fine.
