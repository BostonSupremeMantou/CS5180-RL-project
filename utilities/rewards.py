"""reward helpers — thin for now; real shaping still lives in legacy env."""

from __future__ import annotations


def note() -> str:
    return "reward is computed inside FishTrackingEnv in old_versions for now."
